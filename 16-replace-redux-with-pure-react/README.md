# replace-redux-with-pure-react

Project 15-react-redux-example has nothing wrong, this replacing was made only for testing purposes.
The real motivation for replacing redux with pure react would be
1. To reduce your build in production (not that the build with redux would be so much bigger)
2. Because you don't know / want to use redux in your project.

Overall I think is better to just use redux